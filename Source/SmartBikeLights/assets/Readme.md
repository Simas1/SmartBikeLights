# Graphics sources

Current card artwork lives in `button-icons/` and `control-icons/`. Generate the bitmap font atlases with `node scripts/generate-control-mode-fonts.cjs` from the repository root (requires sharp).

`MenuArrows` and `MenuIcons` source fonts and their SVG components support the touchscreen settings menus. Battery indicators are drawn in code. Legacy light/battery font assets have been removed.
